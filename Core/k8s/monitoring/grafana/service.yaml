apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: axon-graph
  labels:
    app: grafana
    app.kubernetes.io/part-of: axon-graph
spec:
  type: ClusterIP
  selector:
    app: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
