apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: axon-graph
  labels:
    app: prometheus
    app.kubernetes.io/part-of: axon-graph
spec:
  type: ClusterIP
  selector:
    app: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
      protocol: TCP
