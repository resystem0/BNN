apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: axon-graph
  labels:
    app: gateway
    app.kubernetes.io/part-of: axon-graph
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
